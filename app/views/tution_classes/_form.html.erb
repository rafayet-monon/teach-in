<%= form_with(model: tution_class, local: true) do |form| %>
  <% if tution_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tution_class.errors.count, "error") %> prohibited this tution_class from being saved:</h2>

      <ul>
        <% tution_class.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :level %>
    <%= form.number_field :level %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
