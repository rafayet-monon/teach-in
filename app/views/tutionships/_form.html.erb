<%= form_with(model: tutionship, local: true) do |form| %>
  <% if tutionship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tutionship.errors.count, "error") %> prohibited this tutionship from being saved:</h2>

      <ul>
        <% tutionship.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :tution_class_id %>
    <%= form.number_field :tution_class_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
